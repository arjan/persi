-module(persi_table).

-include_lib("persi.hrl").

-export(
   [
    insert/3,
    update/4,
    upsert/4,
    delete/3,
    select/3
   ]).


insert(_, _, _) ->
    ok.

update(_, _, _, _) ->
    ok.

upsert(_, _, _, _) ->
    ok.

delete(_, _, _) ->
    ok.

select(_, _, _) ->
    ok.

